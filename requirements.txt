# ── Core ─────────────────────────────────────────────────────────────────────
lamindb[aws]>=0.76.0
pyarrow>=15.0.0
pandas>=2.0.0
tqdm>=4.0.0
polars

# ── Benchmark engines ─────────────────────────────────────────────────────────
duckdb>=0.10.0
pyiceberg[pyarrow,sql-sqlite]>=0.7.0  # sql-sqlite = local catalog backend
torch>=2.0.0                           # required by lamindb MappedCollection loader
lancedb

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0

# ── AWS (for SageMaker instance type detection) ───────────────────────────────
boto3>=1.34.0
